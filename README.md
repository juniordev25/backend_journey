# Backend Bootcamp (JavaScript)

## Goal
Prepare for a backend development internship in 5 months using JavaScript/Node.js.

## What’s Inside
- `exercises/` — small JS practice files
- `notes/` — daily notes and progress
- `projects/` — larger backend projects (APIs)

## Requirements
- Node.js (LTS)

## Setup
```bash
# install dependencies (if a package.json exists)
npm install
